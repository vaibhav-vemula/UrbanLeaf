{
  "name": "urbanleaf-cre-workflow",
  "version": "1.0.0",
  "description": "Chainlink Runtime Environment workflows for UrbanLeaf AI — AI-powered environmental scoring and automated proposal lifecycle management",
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "npm install && cd evm-score-proposal && npm install && cd ../score-proposal && npm install && cd ../auto-close && npm install",
    "simulate:score": "cre workflow simulate score-proposal -T staging-settings --non-interactive --trigger-index 0 --http-payload @$(pwd)/test/score-proposal-payload.json",
    "simulate:evm": "cre workflow simulate evm-score-proposal -T staging-settings --non-interactive --trigger-index 0 --evm-tx-hash $(cat $(pwd)/test/last-tx-hash.txt)",
    "simulate:autoclose": "cre workflow simulate auto-close -T staging-settings --non-interactive --trigger-index 0"
  },
  "dependencies": {
    "@chainlink/cre-sdk": "latest"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
